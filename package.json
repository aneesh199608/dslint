{
  "name": "dslint",
  "version": "1.0.0",
  "description": "Minimal color token checker for Figma",
  "main": "dist/code.js",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --format=iife --platform=browser --target=es2017 --outfile=dist/code.js && cp src/ui.html dist/ui.html && mkdir -p dist/assets && cp src/assets/*.svg dist/assets/",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.19.12",
    "typescript": "^5.3.2"
  }
}
